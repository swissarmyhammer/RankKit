/// FoundationModelsRanker: shared hybrid-search and ranking primitives (plan.md §1–§3).
///
/// Placeholder scaffold file so the `FoundationModelsRanker` library target compiles.
/// The ported retrieval primitives (BM25, trigram, RRF fusion, embedding
/// seams), the generalized selection tier, and the `Searcher` facade land
/// in the phases plan.md §6 describes; this file is replaced as they do.
