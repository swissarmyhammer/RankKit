{"id":"01KWYG63ERGAXQY8VVMV6CQ80Z","timestamp":"2026-07-07T14:37:18.168270Z","op":"create","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,21 @@\n+---\n+depends_on:\n+- 01KWYG3S45GN3CAR9K0NAVJCS9\n+position_column: todo\n+position_ordinal: '9380'\n+title: Write RankKit README with the one-call example\n+---\n+## What\n+Replace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\n+\n+## Acceptance Criteria\n+- [ ] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n+- [ ] No domain language (chunks/catalogs/metadata) — items and queries only\n+- [ ] States macOS 27 floor and the FoundationModelsRouter dependency\n+\n+## Tests\n+- [ ] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n+- [ ] Run `swift test` — exits 0\n+\n+## Workflow\n+- Use `/tdd` — write the README-snippet test first, then the README.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,21 +0,0 @@\n----\n-depends_on:\n-- 01KWYG3S45GN3CAR9K0NAVJCS9\n-position_column: todo\n-position_ordinal: '9380'\n-title: Write RankKit README with the one-call example\n----\n-## What\n-Replace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\n-\n-## Acceptance Criteria\n-- [ ] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n-- [ ] No domain language (chunks/catalogs/metadata) — items and queries only\n-- [ ] States macOS 27 floor and the FoundationModelsRouter dependency\n-\n-## Tests\n-- [ ] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n-- [ ] Run `swift test` — exits 0\n-\n-## Workflow\n-- Use `/tdd` — write the README-snippet test first, then the README.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KWYGGFC3FG4EDK3RA314XMQC","timestamp":"2026-07-07T14:42:58.051280Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,7 @@\n ---\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n+- 01KWYG46FWSJ4HGVM1149GG40J\n position_column: todo\n position_ordinal: '9380'\n title: Write RankKit README with the one-call example\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,6 @@\n ---\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n-- 01KWYG46FWSJ4HGVM1149GG40J\n position_column: todo\n position_ordinal: '9380'\n title: Write RankKit README with the one-call example\n","transaction_id":null}
{"id":"01KXEG2A6AWEW4SP6603KK64FA","timestamp":"2026-07-13T19:43:04.906601Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -2,8 +2,8 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: todo\n-position_ordinal: '9380'\n+position_column: doing\n+position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -2,8 +2,8 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '9380'\n title: Write RankKit README with the one-call example\n ---\n ## What\n","transaction_id":null}
{"id":"01KXEGR9TK8A4V8RPB1F5ZAF83","timestamp":"2026-07-13T19:55:05.427621Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -10,13 +10,47 @@\n Replace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\n\n ## Acceptance Criteria\n-- [ ] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n-- [ ] No domain language (chunks/catalogs/metadata) — items and queries only\n-- [ ] States macOS 27 floor and the FoundationModelsRouter dependency\n+- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n+- [x] No domain language (chunks/catalogs/metadata) — items and queries only\n+- [x] States macOS 27 floor and the FoundationModelsRouter dependency\n\n ## Tests\n-- [ ] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n-- [ ] Run `swift test` — exits 0\n+- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n+- [x] Run `swift test` — exits 0\n\n ## Workflow\n-- Use `/tdd` — write the README-snippet test first, then the README.\n\\ No newline at end of file\n+- Use `/tdd` — write the README-snippet test first, then the README.\n+\n+## Implementation notes (2026-07-13)\n+\n+`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`,\n+per `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 /\n+^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's\n+zero-config lead example uses `Searcher(items)` as-is (which already defaults\n+to `.default` internally via `Searcher.defaultSessionFactory`), and the\n+\"swap the session\" example shows the explicit `LanguageModelSession(model:\n+.default, instructions:)` form that default already is — proving the seam\n+is a plain argument, never hardcoded, without claiming a nonexistent model\n+variant.\n+\n+Wrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering\n+the lead zero-config example, the explicit session swap, and the\n+embedder+session full-monty shape), all passing against the already-shipped\n+`Searcher`/`SearchItem` API before writing `README.md`, then wrote the\n+README to match.\n+\n+Two rounds of adversarial double-check review caught and fixed real\n+inaccuracies before landing:\n+1. The lead example's comment claimed \"`.score` and per-signal `.signals`\n+   attached\", but `Searcher(items)` with the default session and `.auto`\n+   mode resolves to the selection tier's under-budget pure-pick path for a\n+   3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not\n+   fused retrieval signals. Fixed the comment and tightened the pinning\n+   test to assert the real sentinel values.\n+2. The follow-up \"Modes\" section then claimed `.selection` *always* returns\n+   the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget\n+   fallback returns real fused `score`/`signals` from its retrieval\n+   candidates. Fixed to describe both paths, matching `Searcher.swift`'s\n+   own doc comment.\n+\n+Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -10,47 +10,13 @@\n Replace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\n\n ## Acceptance Criteria\n-- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n-- [x] No domain language (chunks/catalogs/metadata) — items and queries only\n-- [x] States macOS 27 floor and the FoundationModelsRouter dependency\n+- [ ] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n+- [ ] No domain language (chunks/catalogs/metadata) — items and queries only\n+- [ ] States macOS 27 floor and the FoundationModelsRouter dependency\n\n ## Tests\n-- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n-- [x] Run `swift test` — exits 0\n+- [ ] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n+- [ ] Run `swift test` — exits 0\n\n ## Workflow\n-- Use `/tdd` — write the README-snippet test first, then the README.\n-\n-## Implementation notes (2026-07-13)\n-\n-`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`,\n-per `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 /\n-^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's\n-zero-config lead example uses `Searcher(items)` as-is (which already defaults\n-to `.default` internally via `Searcher.defaultSessionFactory`), and the\n-\"swap the session\" example shows the explicit `LanguageModelSession(model:\n-.default, instructions:)` form that default already is — proving the seam\n-is a plain argument, never hardcoded, without claiming a nonexistent model\n-variant.\n-\n-Wrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering\n-the lead zero-config example, the explicit session swap, and the\n-embedder+session full-monty shape), all passing against the already-shipped\n-`Searcher`/`SearchItem` API before writing `README.md`, then wrote the\n-README to match.\n-\n-Two rounds of adversarial double-check review caught and fixed real\n-inaccuracies before landing:\n-1. The lead example's comment claimed \"`.score` and per-signal `.signals`\n-   attached\", but `Searcher(items)` with the default session and `.auto`\n-   mode resolves to the selection tier's under-budget pure-pick path for a\n-   3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not\n-   fused retrieval signals. Fixed the comment and tightened the pinning\n-   test to assert the real sentinel values.\n-2. The follow-up \"Modes\" section then claimed `.selection` *always* returns\n-   the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget\n-   fallback returns real fused `score`/`signals` from its retrieval\n-   candidates. Fixed to describe both paths, matching `Searcher.swift`'s\n-   own doc comment.\n-\n-Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n\\ No newline at end of file\n+- Use `/tdd` — write the README-snippet test first, then the README.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXEH3P1BYJKYN7TQAG0V9YYR","timestamp":"2026-07-13T20:01:18.379689Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n","transaction_id":null}
{"id":"01KXEH40X50QDPRSA0GGSMC489","timestamp":"2026-07-13T20:01:29.509606Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -53,4 +53,8 @@\n    candidates. Fixed to describe both paths, matching `Searcher.swift`'s\n    own doc comment.\n\n-Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n\\ No newline at end of file\n+Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n+\n+## Review Findings (2026-07-13 14:57)\n+\n+- [ ] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\n","reverse_patch":"--- original\n+++ modified\n@@ -53,8 +53,4 @@\n    candidates. Fixed to describe both paths, matching `Searcher.swift`'s\n    own doc comment.\n\n-Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n-\n-## Review Findings (2026-07-13 14:57)\n-\n-- [ ] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\n+Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXEH4K8BC6QBD8F27A205328","timestamp":"2026-07-13T20:01:48.299589Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n","transaction_id":null}
{"id":"01KXEH80121PK5N75XN0BCFRR8","timestamp":"2026-07-13T20:03:39.682964Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -6,55 +6,4 @@\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n-## What\n-Replace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\n-\n-## Acceptance Criteria\n-- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n-- [x] No domain language (chunks/catalogs/metadata) — items and queries only\n-- [x] States macOS 27 floor and the FoundationModelsRouter dependency\n-\n-## Tests\n-- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n-- [x] Run `swift test` — exits 0\n-\n-## Workflow\n-- Use `/tdd` — write the README-snippet test first, then the README.\n-\n-## Implementation notes (2026-07-13)\n-\n-`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`,\n-per `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 /\n-^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's\n-zero-config lead example uses `Searcher(items)` as-is (which already defaults\n-to `.default` internally via `Searcher.defaultSessionFactory`), and the\n-\"swap the session\" example shows the explicit `LanguageModelSession(model:\n-.default, instructions:)` form that default already is — proving the seam\n-is a plain argument, never hardcoded, without claiming a nonexistent model\n-variant.\n-\n-Wrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering\n-the lead zero-config example, the explicit session swap, and the\n-embedder+session full-monty shape), all passing against the already-shipped\n-`Searcher`/`SearchItem` API before writing `README.md`, then wrote the\n-README to match.\n-\n-Two rounds of adversarial double-check review caught and fixed real\n-inaccuracies before landing:\n-1. The lead example's comment claimed \"`.score` and per-signal `.signals`\n-   attached\", but `Searcher(items)` with the default session and `.auto`\n-   mode resolves to the selection tier's under-budget pure-pick path for a\n-   3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not\n-   fused retrieval signals. Fixed the comment and tightened the pinning\n-   test to assert the real sentinel values.\n-2. The follow-up \"Modes\" section then claimed `.selection` *always* returns\n-   the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget\n-   fallback returns real fused `score`/`signals` from its retrieval\n-   candidates. Fixed to describe both paths, matching `Searcher.swift`'s\n-   own doc comment.\n-\n-Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n-\n-## Review Findings (2026-07-13 14:57)\n-\n-- [ ] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\n+## What\\nReplace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\\n\\n## Acceptance Criteria\\n- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\\n- [x] No domain language (chunks/catalogs/metadata) — items and queries only\\n- [x] States macOS 27 floor and the FoundationModelsRouter dependency\\n\\n## Tests\\n- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\\n- [x] Run `swift test` — exits 0\\n\\n## Workflow\\n- Use `/tdd` — write the README-snippet test first, then the README.\\n\\n## Implementation notes (2026-07-13)\\n\\n`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`,\\nper `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 /\\n^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's\\nzero-config lead example uses `Searcher(items)` as-is (which already defaults\\nto `.default` internally via `Searcher.defaultSessionFactory`), and the\\n\\\"swap the session\\\" example shows the explicit `LanguageModelSession(model:\\n.default, instructions:)` form that default already is — proving the seam\\nis a plain argument, never hardcoded, without claiming a nonexistent model\\nvariant.\\n\\nWrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering\\nthe lead zero-config example, the explicit session swap, and the\\nembedder+session full-monty shape), all passing against the already-shipped\\n`Searcher`/`SearchItem` API before writing `README.md`, then wrote the\\nREADME to match.\\n\\nTwo rounds of adversarial double-check review caught and fixed real\\ninaccuracies before landing:\\n1. The lead example's comment claimed \\\"`.score` and per-signal `.signals`\\n   attached\\\", but `Searcher(items)` with the default session and `.auto`\\n   mode resolves to the selection tier's under-budget pure-pick path for a\\n   3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not\\n   fused retrieval signals. Fixed the comment and tightened the pinning\\n   test to assert the real sentinel values.\\n2. The follow-up \\\"Modes\\\" section then claimed `.selection` *always* returns\\n   the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget\\n   fallback returns real fused `score`/`signals` from its retrieval\\n   candidates. Fixed to describe both paths, matching `Searcher.swift`'s\\n   own doc comment.\\n\\nFinal state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\\n\\n## Review Findings (2026-07-13 14:57)\\n\\n- [x] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\\n\\n## Review fix (2026-07-13, round 2)\\n\\nReplaced the inline `[grep, glob, watch]` `SearchItem` literal in\\n`leadExampleFindsGrepForATodoCommentsQuery` with `SearcherTests.toolItems`\\n(no access-level change needed — both types are internal/default access in\\nthe same test target). The other two tests in the file use a different\\n2-item (grep/glob only) array that is not identical to `toolItems`, so they\\nwere left as-is — out of scope for this finding.\\n\\n`swift build`: exit 0. `swift test`: 193/193 passed, including\\n`swift test --filter ReadmeExampleTests` (3/3). Adversarial double-check\\nagent reviewed the diff and returned PASS.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -6,4 +6,55 @@\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n-## What\\nReplace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\\n\\n## Acceptance Criteria\\n- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\\n- [x] No domain language (chunks/catalogs/metadata) — items and queries only\\n- [x] States macOS 27 floor and the FoundationModelsRouter dependency\\n\\n## Tests\\n- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\\n- [x] Run `swift test` — exits 0\\n\\n## Workflow\\n- Use `/tdd` — write the README-snippet test first, then the README.\\n\\n## Implementation notes (2026-07-13)\\n\\n`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`,\\nper `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 /\\n^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's\\nzero-config lead example uses `Searcher(items)` as-is (which already defaults\\nto `.default` internally via `Searcher.defaultSessionFactory`), and the\\n\\\"swap the session\\\" example shows the explicit `LanguageModelSession(model:\\n.default, instructions:)` form that default already is — proving the seam\\nis a plain argument, never hardcoded, without claiming a nonexistent model\\nvariant.\\n\\nWrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering\\nthe lead zero-config example, the explicit session swap, and the\\nembedder+session full-monty shape), all passing against the already-shipped\\n`Searcher`/`SearchItem` API before writing `README.md`, then wrote the\\nREADME to match.\\n\\nTwo rounds of adversarial double-check review caught and fixed real\\ninaccuracies before landing:\\n1. The lead example's comment claimed \\\"`.score` and per-signal `.signals`\\n   attached\\\", but `Searcher(items)` with the default session and `.auto`\\n   mode resolves to the selection tier's under-budget pure-pick path for a\\n   3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not\\n   fused retrieval signals. Fixed the comment and tightened the pinning\\n   test to assert the real sentinel values.\\n2. The follow-up \\\"Modes\\\" section then claimed `.selection` *always* returns\\n   the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget\\n   fallback returns real fused `score`/`signals` from its retrieval\\n   candidates. Fixed to describe both paths, matching `Searcher.swift`'s\\n   own doc comment.\\n\\nFinal state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\\n\\n## Review Findings (2026-07-13 14:57)\\n\\n- [x] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\\n\\n## Review fix (2026-07-13, round 2)\\n\\nReplaced the inline `[grep, glob, watch]` `SearchItem` literal in\\n`leadExampleFindsGrepForATodoCommentsQuery` with `SearcherTests.toolItems`\\n(no access-level change needed — both types are internal/default access in\\nthe same test target). The other two tests in the file use a different\\n2-item (grep/glob only) array that is not identical to `toolItems`, so they\\nwere left as-is — out of scope for this finding.\\n\\n`swift build`: exit 0. `swift test`: 193/193 passed, including\\n`swift test --filter ReadmeExampleTests` (3/3). Adversarial double-check\\nagent reviewed the diff and returned PASS.\n\\ No newline at end of file\n+## What\n+Replace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\n+\n+## Acceptance Criteria\n+- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n+- [x] No domain language (chunks/catalogs/metadata) — items and queries only\n+- [x] States macOS 27 floor and the FoundationModelsRouter dependency\n+\n+## Tests\n+- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n+- [x] Run `swift test` — exits 0\n+\n+## Workflow\n+- Use `/tdd` — write the README-snippet test first, then the README.\n+\n+## Implementation notes (2026-07-13)\n+\n+`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`,\n+per `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 /\n+^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's\n+zero-config lead example uses `Searcher(items)` as-is (which already defaults\n+to `.default` internally via `Searcher.defaultSessionFactory`), and the\n+\"swap the session\" example shows the explicit `LanguageModelSession(model:\n+.default, instructions:)` form that default already is — proving the seam\n+is a plain argument, never hardcoded, without claiming a nonexistent model\n+variant.\n+\n+Wrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering\n+the lead zero-config example, the explicit session swap, and the\n+embedder+session full-monty shape), all passing against the already-shipped\n+`Searcher`/`SearchItem` API before writing `README.md`, then wrote the\n+README to match.\n+\n+Two rounds of adversarial double-check review caught and fixed real\n+inaccuracies before landing:\n+1. The lead example's comment claimed \"`.score` and per-signal `.signals`\n+   attached\", but `Searcher(items)` with the default session and `.auto`\n+   mode resolves to the selection tier's under-budget pure-pick path for a\n+   3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not\n+   fused retrieval signals. Fixed the comment and tightened the pinning\n+   test to assert the real sentinel values.\n+2. The follow-up \"Modes\" section then claimed `.selection` *always* returns\n+   the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget\n+   fallback returns real fused `score`/`signals` from its retrieval\n+   candidates. Fixed to describe both paths, matching `Searcher.swift`'s\n+   own doc comment.\n+\n+Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n+\n+## Review Findings (2026-07-13 14:57)\n+\n+- [ ] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\n","transaction_id":null}
{"id":"01KXEH82DGD4MM8VH8NFXH71JP","timestamp":"2026-07-13T20:03:42.128704Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -1,4 +1,14 @@\n ---\n+comments:\n+- actor: claude-code\n+  id: 01kxeh82dgvyan258z74fwdf38\n+  text: |-\n+    Fixed the outstanding review finding: Tests/RankKitTests/ReadmeExampleTests.swift's leadExampleFindsGrepForATodoCommentsQuery now uses `SearcherTests.toolItems` instead of a duplicated inline grep/glob/watch SearchItem array. No access-level changes were needed (both types are internal, same test target). The other two tests' 2-item arrays were left untouched — not identical to toolItems, out of scope.\n+\n+    Verification: swift build exit 0. swift test 193/193 passed (full suite), swift test --filter ReadmeExampleTests 3/3 passed. Adversarial double-check agent reviewed the change and returned PASS.\n+\n+    Checklist item flipped to [x]. Leaving task in doing per /implement workflow — ready for /review to pull it back into review.\n+  timestamp: 2026-07-13T20:03:42.128346+00:00\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n","reverse_patch":"--- original\n+++ modified\n@@ -1,14 +1,4 @@\n ---\n-comments:\n-- actor: claude-code\n-  id: 01kxeh82dgvyan258z74fwdf38\n-  text: |-\n-    Fixed the outstanding review finding: Tests/RankKitTests/ReadmeExampleTests.swift's leadExampleFindsGrepForATodoCommentsQuery now uses `SearcherTests.toolItems` instead of a duplicated inline grep/glob/watch SearchItem array. No access-level changes were needed (both types are internal, same test target). The other two tests' 2-item arrays were left untouched — not identical to toolItems, out of scope.\n-\n-    Verification: swift build exit 0. swift test 193/193 passed (full suite), swift test --filter ReadmeExampleTests 3/3 passed. Adversarial double-check agent reviewed the change and returned PASS.\n-\n-    Checklist item flipped to [x]. Leaving task in doing per /implement workflow — ready for /review to pull it back into review.\n-  timestamp: 2026-07-13T20:03:42.128346+00:00\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n","transaction_id":null}
{"id":"01KXEHA72ZE0CVER5J2D78BSFB","timestamp":"2026-07-13T20:04:52.447537Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -16,4 +16,39 @@\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n-## What\\nReplace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\\n\\n## Acceptance Criteria\\n- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\\n- [x] No domain language (chunks/catalogs/metadata) — items and queries only\\n- [x] States macOS 27 floor and the FoundationModelsRouter dependency\\n\\n## Tests\\n- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\\n- [x] Run `swift test` — exits 0\\n\\n## Workflow\\n- Use `/tdd` — write the README-snippet test first, then the README.\\n\\n## Implementation notes (2026-07-13)\\n\\n`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`,\\nper `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 /\\n^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's\\nzero-config lead example uses `Searcher(items)` as-is (which already defaults\\nto `.default` internally via `Searcher.defaultSessionFactory`), and the\\n\\\"swap the session\\\" example shows the explicit `LanguageModelSession(model:\\n.default, instructions:)` form that default already is — proving the seam\\nis a plain argument, never hardcoded, without claiming a nonexistent model\\nvariant.\\n\\nWrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering\\nthe lead zero-config example, the explicit session swap, and the\\nembedder+session full-monty shape), all passing against the already-shipped\\n`Searcher`/`SearchItem` API before writing `README.md`, then wrote the\\nREADME to match.\\n\\nTwo rounds of adversarial double-check review caught and fixed real\\ninaccuracies before landing:\\n1. The lead example's comment claimed \\\"`.score` and per-signal `.signals`\\n   attached\\\", but `Searcher(items)` with the default session and `.auto`\\n   mode resolves to the selection tier's under-budget pure-pick path for a\\n   3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not\\n   fused retrieval signals. Fixed the comment and tightened the pinning\\n   test to assert the real sentinel values.\\n2. The follow-up \\\"Modes\\\" section then claimed `.selection` *always* returns\\n   the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget\\n   fallback returns real fused `score`/`signals` from its retrieval\\n   candidates. Fixed to describe both paths, matching `Searcher.swift`'s\\n   own doc comment.\\n\\nFinal state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\\n\\n## Review Findings (2026-07-13 14:57)\\n\\n- [x] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\\n\\n## Review fix (2026-07-13, round 2)\\n\\nReplaced the inline `[grep, glob, watch]` `SearchItem` literal in\\n`leadExampleFindsGrepForATodoCommentsQuery` with `SearcherTests.toolItems`\\n(no access-level change needed — both types are internal/default access in\\nthe same test target). The other two tests in the file use a different\\n2-item (grep/glob only) array that is not identical to `toolItems`, so they\\nwere left as-is — out of scope for this finding.\\n\\n`swift build`: exit 0. `swift test`: 193/193 passed, including\\n`swift test --filter ReadmeExampleTests` (3/3). Adversarial double-check\\nagent reviewed the diff and returned PASS.\n\\ No newline at end of file\n+## What\n+Replace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\n+\n+## Acceptance Criteria\n+- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n+- [x] No domain language (chunks/catalogs/metadata) — items and queries only\n+- [x] States macOS 27 floor and the FoundationModelsRouter dependency\n+\n+## Tests\n+- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n+- [x] Run `swift test` — exits 0\n+\n+## Workflow\n+- Use `/tdd` — write the README-snippet test first, then the README.\n+\n+## Implementation notes (2026-07-13)\n+\n+`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`, per `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 / ^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's zero-config lead example uses `Searcher(items)` as-is (which already defaults to `.default` internally via `Searcher.defaultSessionFactory`), and the \"swap the session\" example shows the explicit `LanguageModelSession(model: .default, instructions:)` form that default already is — proving the seam is a plain argument, never hardcoded, without claiming a nonexistent model variant.\n+\n+Wrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering the lead zero-config example, the explicit session swap, and the embedder+session full-monty shape), all passing against the already-shipped `Searcher`/`SearchItem` API before writing `README.md`, then wrote the README to match.\n+\n+Two rounds of adversarial double-check review caught and fixed real inaccuracies before landing:\n+1. The lead example's comment claimed \"`.score` and per-signal `.signals` attached\", but `Searcher(items)` with the default session and `.auto` mode resolves to the selection tier's under-budget pure-pick path for a 3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not fused retrieval signals. Fixed the comment and tightened the pinning test to assert the real sentinel values.\n+2. The follow-up \"Modes\" section then claimed `.selection` *always* returns the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget fallback returns real fused `score`/`signals` from its retrieval candidates. Fixed to describe both paths, matching `Searcher.swift`'s own doc comment.\n+\n+Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n+\n+## Review Findings (2026-07-13 14:57)\n+\n+- [x] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\n+\n+## Review fix (2026-07-13, round 2)\n+\n+Replaced the inline `[grep, glob, watch]` `SearchItem` literal in `leadExampleFindsGrepForATodoCommentsQuery` with `SearcherTests.toolItems` (no access-level change needed — both types are internal/default access in the same test target). The other two tests in the file use a different 2-item (grep/glob only) array that is not identical to `toolItems`, so they were left as-is — out of scope for this finding.\n+\n+`swift build`: exit 0. `swift test`: 193/193 passed, including `swift test --filter ReadmeExampleTests` (3/3). Adversarial double-check agent reviewed the diff and returned PASS.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -16,39 +16,4 @@\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n-## What\n-Replace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\n-\n-## Acceptance Criteria\n-- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\n-- [x] No domain language (chunks/catalogs/metadata) — items and queries only\n-- [x] States macOS 27 floor and the FoundationModelsRouter dependency\n-\n-## Tests\n-- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\n-- [x] Run `swift test` — exits 0\n-\n-## Workflow\n-- Use `/tdd` — write the README-snippet test first, then the README.\n-\n-## Implementation notes (2026-07-13)\n-\n-`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`, per `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 / ^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's zero-config lead example uses `Searcher(items)` as-is (which already defaults to `.default` internally via `Searcher.defaultSessionFactory`), and the \"swap the session\" example shows the explicit `LanguageModelSession(model: .default, instructions:)` form that default already is — proving the seam is a plain argument, never hardcoded, without claiming a nonexistent model variant.\n-\n-Wrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering the lead zero-config example, the explicit session swap, and the embedder+session full-monty shape), all passing against the already-shipped `Searcher`/`SearchItem` API before writing `README.md`, then wrote the README to match.\n-\n-Two rounds of adversarial double-check review caught and fixed real inaccuracies before landing:\n-1. The lead example's comment claimed \"`.score` and per-signal `.signals` attached\", but `Searcher(items)` with the default session and `.auto` mode resolves to the selection tier's under-budget pure-pick path for a 3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not fused retrieval signals. Fixed the comment and tightened the pinning test to assert the real sentinel values.\n-2. The follow-up \"Modes\" section then claimed `.selection` *always* returns the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget fallback returns real fused `score`/`signals` from its retrieval candidates. Fixed to describe both paths, matching `Searcher.swift`'s own doc comment.\n-\n-Final state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\n-\n-## Review Findings (2026-07-13 14:57)\n-\n-- [x] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\n-\n-## Review fix (2026-07-13, round 2)\n-\n-Replaced the inline `[grep, glob, watch]` `SearchItem` literal in `leadExampleFindsGrepForATodoCommentsQuery` with `SearcherTests.toolItems` (no access-level change needed — both types are internal/default access in the same test target). The other two tests in the file use a different 2-item (grep/glob only) array that is not identical to `toolItems`, so they were left as-is — out of scope for this finding.\n-\n-`swift build`: exit 0. `swift test`: 193/193 passed, including `swift test --filter ReadmeExampleTests` (3/3). Adversarial double-check agent reviewed the diff and returned PASS.\n\\ No newline at end of file\n+## What\\nReplace the scaffold's `README.md` stub with the real library README (use the `/make-readme` skill, `library` mode — no logo, leads with an inline runnable usage example). The lead example is plan.md §3a's: a `SearchItem` list, `Searcher(items)` on the `.fast` default, one `search(...)` call; then the `.default` swap and the Router/embedder full-monty variant. Document graceful degradation and the `.retrieval`/`.selection`/`.auto` modes in one short section. Link `Examples/FullMonty`.\\n\\n## Acceptance Criteria\\n- [x] README's lead code block compiles when pasted into `Examples/FullMontyCore` context (verified by keeping it in sync with `ExamplesSmokeTests` fixtures)\\n- [x] No domain language (chunks/catalogs/metadata) — items and queries only\\n- [x] States macOS 27 floor and the FoundationModelsRouter dependency\\n\\n## Tests\\n- [x] Add a `ReadmeExampleTests.swift` case that exercises the exact README snippet shape (fixture items + fake session) so the documented API can't drift silently\\n- [x] Run `swift test` — exits 0\\n\\n## Workflow\\n- Use `/tdd` — write the README-snippet test first, then the README.\\n\\n## Implementation notes (2026-07-13)\\n\\n`.fast` doesn't exist in the installed SDK (only `SystemLanguageModel.default`,\\nper `Searcher.swift`'s header note and the adaptation carried from ^navjcs9 /\\n^49gg40j), so there's no `.fast`→`.default` swap to demonstrate. The README's\\nzero-config lead example uses `Searcher(items)` as-is (which already defaults\\nto `.default` internally via `Searcher.defaultSessionFactory`), and the\\n\\\"swap the session\\\" example shows the explicit `LanguageModelSession(model:\\n.default, instructions:)` form that default already is — proving the seam\\nis a plain argument, never hardcoded, without claiming a nonexistent model\\nvariant.\\n\\nWrote `Tests/RankKitTests/ReadmeExampleTests.swift` first (3 cases covering\\nthe lead zero-config example, the explicit session swap, and the\\nembedder+session full-monty shape), all passing against the already-shipped\\n`Searcher`/`SearchItem` API before writing `README.md`, then wrote the\\nREADME to match.\\n\\nTwo rounds of adversarial double-check review caught and fixed real\\ninaccuracies before landing:\\n1. The lead example's comment claimed \\\"`.score` and per-signal `.signals`\\n   attached\\\", but `Searcher(items)` with the default session and `.auto`\\n   mode resolves to the selection tier's under-budget pure-pick path for a\\n   3-item list, which returns the `score: 1.0`/`signals: nil` sentinel, not\\n   fused retrieval signals. Fixed the comment and tightened the pinning\\n   test to assert the real sentinel values.\\n2. The follow-up \\\"Modes\\\" section then claimed `.selection` *always* returns\\n   the `1.0`/`nil` sentinel, missing that `SelectionTier`'s over-budget\\n   fallback returns real fused `score`/`signals` from its retrieval\\n   candidates. Fixed to describe both paths, matching `Searcher.swift`'s\\n   own doc comment.\\n\\nFinal state: `swift build` exit 0, `swift test` 193/193 passed (fresh run).\\n\\n## Review Findings (2026-07-13 14:57)\\n\\n- [x] `Tests/RankKitTests/ReadmeExampleTests.swift:37` — Recreates SearchItem array (grep, glob, watch) that is identical to SearcherTests.toolItems, rather than reusing the shared test fixture already defined in the same test target. Use SearcherTests.toolItems directly: `let items = SearcherTests.toolItems`.\\n\\n## Review fix (2026-07-13, round 2)\\n\\nReplaced the inline `[grep, glob, watch]` `SearchItem` literal in\\n`leadExampleFindsGrepForATodoCommentsQuery` with `SearcherTests.toolItems`\\n(no access-level change needed — both types are internal/default access in\\nthe same test target). The other two tests in the file use a different\\n2-item (grep/glob only) array that is not identical to `toolItems`, so they\\nwere left as-is — out of scope for this finding.\\n\\n`swift build`: exit 0. `swift test`: 193/193 passed, including\\n`swift test --filter ReadmeExampleTests` (3/3). Adversarial double-check\\nagent reviewed the diff and returned PASS.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KXEHWN7AGM1QWRMKWREG5X21","timestamp":"2026-07-13T20:14:56.746566Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -12,7 +12,7 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -12,7 +12,7 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n","transaction_id":null}
{"id":"01KXEHWQWT97D3N60T9HG7BSSS","timestamp":"2026-07-13T20:14:59.482685Z","op":"update","item_id":"01KWYG63EQXRHB63RD49T8VA7Z","forward_patch":"--- original\n+++ modified\n@@ -12,8 +12,8 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: 8c80\n title: Write RankKit README with the one-call example\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -12,8 +12,8 @@\n depends_on:\n - 01KWYG3S45GN3CAR9K0NAVJCS9\n - 01KWYG46FWSJ4HGVM1149GG40J\n-position_column: done\n-position_ordinal: 8c80\n+position_column: review\n+position_ordinal: '80'\n title: Write RankKit README with the one-call example\n ---\n ## What\n","transaction_id":null}
