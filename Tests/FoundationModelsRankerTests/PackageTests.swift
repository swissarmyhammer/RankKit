import FoundationModelsRanker
import Testing

/// Placeholder scaffold test proving the library target builds and is importable.
///
/// Proves the `FoundationModelsRanker` library target builds, links, and is importable from a
/// Swift Testing test target (plan.md §3). Replaced by real tests as the
/// ported retrieval primitives land (plan.md §5).
@Test func moduleImportsAndBuilds() {
    #expect(Bool(true))
}
